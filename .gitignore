# Root-level .gitignore for shopcart monorepo (frontend + backend)

# Node dependencies (anywhere in repo)
**/node_modules/
**/.pnp/
.pnp.*

# Build outputs (Next.js / Node.js / general)
**/.next/
**/out/
**/dist/
**/build/

# Vercel / deployment
**/.vercel/

# Env files (keep them private)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Lockfiles (optional — remove `#` if you don’t want to commit them)
# pnpm-lock.yaml
# package-lock.json
# yarn.lock

# Editor / OS files
.DS_Store
Thumbs.db
.vscode/
.idea/

# TypeScript build cache
**/*.tsbuildinfo
frontend/env.local
